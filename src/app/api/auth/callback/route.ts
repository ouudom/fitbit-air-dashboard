import { cookies } from 'next/headers';import { NextResponse } from 'next/server';import { exchangeCode } from '../../../../lib/oauth';
export async function GET(req:Request){const u=new URL(req.url),code=u.searchParams.get('code'),state=u.searchParams.get('state'),saved=(await cookies()).get('oauth_state')?.value;if(!code||!state||state!==saved)return new NextResponse('Invalid OAuth state',{status:400});try{await exchangeCode(code);const r=NextResponse.redirect(new URL('/dashboard?connected=1',req.url));r.cookies.delete('oauth_state');return r}catch(e){return new NextResponse((e as Error).message,{status:500})}}
