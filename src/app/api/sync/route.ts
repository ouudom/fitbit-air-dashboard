import { NextRequest,NextResponse } from 'next/server';import { config } from '../../../lib/config';import { authenticated } from '../../../lib/oauth';import { runSync } from '../../../lib/sync';
export async function POST(req:NextRequest){if(!await authenticated())return NextResponse.json({error:'NOT_AUTHENTICATED'},{status:401});const body=await req.json().catch(()=>({}));try{return NextResponse.json(await runSync(Number(body.days??config.syncDays)))}catch(e){return NextResponse.json({error:(e as Error).message},{status:500})}}
