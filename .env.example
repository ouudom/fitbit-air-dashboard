# Use production only behind HTTPS. Production startup rejects placeholder or
# missing OAuth/encryption settings.
APP_ENV=local
APP_TIMEZONE=Asia/Phnom_Penh
LOG_LEVEL=INFO
SETUP_TOKEN=replace-with-a-long-random-one-time-secret

DATABASE_URL=postgresql+asyncpg://lifestats:change-me@localhost:5432/lifestats
POSTGRES_PASSWORD=change-me
REDIS_URL=redis://localhost:6379/0

SESSION_LIFETIME_HOURS=168

# Public OAuth discovery URLs for per-user MCP authorization.
# Production requires HTTPS. Both should point at this deployment's Caddy host.
MCP_PUBLIC_URL=http://localhost:3000/mcp
MCP_OAUTH_ISSUER_URL=http://localhost:3000

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
REDIRECT_URI=http://localhost:3000/api/v1/oauth/google-health/callback
SCOPES="https://www.googleapis.com/auth/googlehealth.activity_and_fitness.readonly https://www.googleapis.com/auth/googlehealth.health_metrics_and_measurements.readonly https://www.googleapis.com/auth/googlehealth.nutrition.readonly https://www.googleapis.com/auth/googlehealth.sleep.readonly https://www.googleapis.com/auth/googlehealth.ecg.readonly https://www.googleapis.com/auth/googlehealth.irn.readonly https://www.googleapis.com/auth/googlehealth.location.readonly"
# Generate independent random values. Keep TOKEN_ENCRYPTION_KEY permanently;
# changing it makes stored Google tokens unreadable.
TOKEN_ENCRYPTION_KEY=

# Google Health webhooks. Keep disabled until polling activation gates pass.
GOOGLE_CLOUD_PROJECT_NUMBER=
GOOGLE_HEALTH_SUBSCRIBER_ID=
GOOGLE_HEALTH_WEBHOOK_URL=
GOOGLE_HEALTH_WEBHOOK_AUTH_SECRET=
GOOGLE_HEALTH_WEBHOOK_ENABLED=false

# Retained only to decrypt legacy Laravel token payloads during cutover.
APP_KEY=
