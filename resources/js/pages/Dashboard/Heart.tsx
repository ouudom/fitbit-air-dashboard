import { Head } from '@inertiajs/react';
import { BarChart, Empty, MetricCard, PageHeader, Section } from '../../components/ui';
import type { VitalPoint } from '../../types';
export default function Heart({vitals={}}:{vitals:Record<string,VitalPoint[]>}){const latest=(t:string)=>vitals[t]?.[0]?.value??'—';const chart=(t:string)=>vitals[t]?.slice(0,30).reverse().map(x=>({label:x.date?.slice(5)??'—',value:x.value}))??[];return <><Head title="Heart & vitals"/><main><PageHeader eyebrow="Health" title="Heart & vitals" subtitle="Trends from Google Health sensors and derived metrics."/><section className="metricGrid"><MetricCard label="Resting heart rate" value={latest('daily-resting-heart-rate')} unit="bpm" detail="Latest"/><MetricCard label="HRV" value={latest('daily-heart-rate-variability')} unit="ms" detail="Latest"/><MetricCard label="SpO₂" value={latest('daily-oxygen-saturation')} unit="%" detail="Latest"/><MetricCard label="Breathing rate" value={latest('daily-respiratory-rate')} unit="/min" detail="Latest"/></section><Section title="Resting heart rate"><BarChart data={chart('daily-resting-heart-rate')} color="#d56d6d" unit="bpm"/></Section><Section title="Heart-rate variability"><BarChart data={chart('daily-heart-rate-variability')} color="#8c79b8" unit="ms"/></Section><Section title="Oxygen saturation"><BarChart data={chart('daily-oxygen-saturation')} color="#4d9a9a" unit="%"/></Section><Section title="Raw heart-rate samples" description="Available after a full raw sync.">{vitals['heart-rate']?.length?<table><thead><tr><th>Time</th><th>BPM</th><th>Source</th></tr></thead><tbody>{vitals['heart-rate'].slice(0,30).map(r=><tr key={r.id}><td>{r.time?new Date(r.time).toLocaleString():'—'}</td><td>{r.value??'—'}</td><td>{r.source}</td></tr>)}</tbody></table>:<Empty>No raw heart-rate samples.</Empty>}</Section></main></>}
